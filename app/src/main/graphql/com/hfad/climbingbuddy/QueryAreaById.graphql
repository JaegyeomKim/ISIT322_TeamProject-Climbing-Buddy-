query QueryAreaById($id:ID!) {
    area(uuid: $id) {
        uuid
        area_name
        metadata {
            lng
            lat
        }
        totalClimbs
        content {
            description
        }
    }
}
